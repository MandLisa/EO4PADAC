library(terra)

# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2023.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2023.tif", overwrite=TRUE)


# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2022.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2022.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2021.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2021.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2021.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2021.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2020.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2020.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2018.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2018.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2016.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2016.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2015.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2015.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2014.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2014.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2013.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2013.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2012.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2012.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2011.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2011.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2010.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2010.tif", overwrite=TRUE)






# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2009.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2009.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2008.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2008.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2007.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2007.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2006.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2006.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2005.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2005.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2004.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2004.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2003.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2003.tif", overwrite=TRUE)


# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2002.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2002.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2001.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2001.tif", overwrite=TRUE)


# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_2000.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover2000.tif", overwrite=TRUE)


# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1999.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1999.tif", overwrite=TRUE)


# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1998.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1998.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1997.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1997.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1996.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1996.tif", overwrite=TRUE)





# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1995.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1995.tif", overwrite=TRUE)





# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1994.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1994.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1993.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1993.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1992.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1992.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1991.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1991.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1990.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1990.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1989.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1989.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1988.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1988.tif", overwrite=TRUE)




# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1987.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1987.tif", overwrite=TRUE)



# Load the raster (assuming your raster file is called "your_raster.tif")
r <- rast("~/eo_nas/EO4Alps/level3_predictions/l2_mask/mosaic_1986.tif")

# Add bands 6 and 7
combined_band <- r[[6]] + r[[7]]

# Export the combined band
writeRaster(combined_band, "~/eo_nas/EO4Alps/level3_predictions/l2_mask/tree_only/treecover1986.tif", overwrite=TRUE)
